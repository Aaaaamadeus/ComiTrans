# ComiTrans UI Style Guide

> 版本：0.1，可审阅初稿。
> 目标：为以日漫为主的漫画自动翻译与嵌字工具，建立现代黑白漫画杂志风的桌面 UI。
> 适用：现有 Python / PySide6 桌面应用。已核对 requirements.txt 中的 PySide6>=6.7.0；旧版 Vue 3 / Element Plus / Vite 前端不属于本次目标。
> 已查看自动处理主界面截图，尚未查看完整源码与精修页面。下文页面结构是设计提案，不能当作现有实现描述。

## 1. 已确认的设计决策

- 风格：漫画杂志排版骨架，加入少量分镜、气泡、网点和出框元素。
- 配色：纯黑、白、无彩灰。应用自绘 UI 不使用任何彩色强调色。
- 质感：现代漫画印刷；干净、锋利、规整，不做旧、不模拟手绘抖线。
- 工作流程：批量导入 → 自动处理 → 检查结果 → 精修 → 导出。
- 精修布局：左侧页面、中间画布、右侧编辑面板，两侧支持折叠与宽度调整。
- 自动处理反馈：流程进度与详细日志两个区域独立展示、同步更新；日志真实流式输出。
- 控件：以直角矩形为基础，少量切角与漫画气泡用于重点反馈。
- 动效：日常交互克制，关键反馈适度漫画化。
- 改造范围：允许大幅重构导航、布局和入口。保留既有业务能力、文件兼容与任务语义，先核对现有功能再迁移。
- 允许 AI 制作配套资产，制作方式与使用边界见第 10 节。

下文尺寸、字体组合、资产预算、导航命名属于设计默认值，可依据真实窗口验证调整；调整时保持上述已确认方向。

## 2. 视觉原则

让用户感受到“漫画编辑部的桌面工具”：黑色标题与分格确立秩序，局部漫画符号提供个性，正文与作品保持清楚。

优先级：作品判断与文字阅读 > 操作可发现性 > 状态辨识 > 漫画装饰。

- 漫画感来自构图、黑白分配、标题与局部网点，不能只把通用后台改成黑白。
- 页面只建立必要分区。禁止容器、卡片、分组框反复套框。
- 主要轮廓粗、控件边框中等、内部分隔细，不能所有元素同等粗重。
- 同屏保留一处明确主操作；运行中将当前任务状态与停止操作置于可见位置。
- 装饰不进入漫画画布、译文输入区、日志正文区，不遮挡任何交互目标。
- 中文作为主要界面语言；“OCR”“API”等必要缩写保留。不用大量日文拟声字代替功能名称。
- 用户漫画、生成结果及嵌字颜色不受 UI 黑白限制，不为统一皮肤而转换原图色彩。

## 3. 信息架构与页面

### 3.1 应用外壳

- 默认保留系统窗口标题栏、拖动与缩放行为；原生标题栏和文件选择器可跟随系统配色。
- 标题栏下使用约 56 个逻辑像素高的应用栏：左侧 ComiTrans 字标，中部主要工作区导航，右侧设置、诊断与帮助。
- 主导航建议为“自动处理”“嵌字精修”；精修尚无可用文件时显示可操作的导入引导或明确不可用原因。
- API、模型、默认输出等放入设置中的清晰分组；诊断作为全局入口，详细运行日志仍保留在当前任务页面。
- 将现有反馈、输出目录、停止等功能逐一映射到新入口，不能在重排中消失。
- 使用一个共享任务状态来源，切换页面不重启、不重复创建任务。
- 本版不要求新增账户、云同步、历史数据库、多任务并发等业务系统。

### 3.2 自动处理工作区

采用左侧文件队列、右侧处理主体，不机械套用精修三栏。

| 区域 | 内容与行为 | 默认尺寸 |
| --- | --- | --- |
| 左侧队列 | 导入文件/文件夹、缩略图、文件名、处理状态、移除与清空 | 宽 240，范围 200–320 |
| 任务操作条 | 语言、输出位置、常用预设、主操作；高级选项折叠 | 内容决定高度，常态约 64–88 |
| 流程进度区 | 总体进度、页数、当前阶段、阶段状态 | 常态约 120–160 |
| 图像预览 | 原图/译图切换，加载后按比例显示 | 使用主要剩余空间 |
| 详细日志区 | 日志工具条与持续追加内容 | 默认高 220，展开时至少容纳约 8 行 |

- 预览与日志之间使用可拖动分隔；保存用户调整后的比例。
- 队列无文件时，主体展示白底黑线的导入引导插画与“添加漫画”按钮，不保留巨大的纯黑空洞。
- 初始化或预热时，无图可预览可将更多空间交给进度与日志；一旦用户手动调整比例，不再自动抢占布局。
- 已有预览区域使用中性深灰背景，区分白色纸页与应用面板。
- 当前截图中的七阶段保留语义：读取图片、气泡检测、OCR 识别、AI 翻译、背景修复、中文排版、保存输出。正式名称与实际后端事件核对；不虚构或强制执行某个阶段。
- 宽窗口可横向排列七阶段；空间不足时改为两行有序分格或紧凑阶段列表，不能不断缩小字号。

### 3.3 检查结果与嵌字精修

- 结果检查通过队列状态、原图/译图预览与“进入精修”连接工作流程，不必增加无内容的独立仪表盘。
- 精修采用三栏：左侧页面缩略图默认 220；中央画布弹性扩展；右侧编辑面板默认 320。单位均为逻辑像素。
- 右侧按“原文与译文”“字体与排版”“位置与区域”等现有能力组织，常用项展开、低频项折叠。
- 顶部工具条收纳选择、缩放、适应页面、对照、撤销/重做等实际支持的操作；禁止添加无法使用的假按钮。
- 选区使用黑白双层轮廓，使其在深色和浅色画面上均可见；控制点可见尺寸约 8，命中范围至少 16。
- 画布的平移、缩放和文字选区不得被装饰层截获。
- 当前选中页、未保存修改与错误页使用不同标记，并配文字说明。
- UI 字体与漫画输出字体完全分离；更换应用皮肤不能改变漫画嵌字结果。
- 导出成功只在文件写入确实成功后展示，部分失败需列出失败数量与后续操作。

### 3.4 设置与诊断

- 设置使用分组表单和细分隔线，避免每一个字段单独套卡片。
- 长路径显示末端文件名或目录名，同时提供完整路径提示与复制操作。
- 错误诊断显示可理解的原因、影响与可执行操作；技术堆栈按需展开。
- 删除、覆盖已有文件等保留现有必要保护；普通导航、换肤和展开面板不增加确认弹窗。

## 4. 设计令牌

所有几何值以 Qt 逻辑像素为准，不直接按物理屏幕像素乘设备缩放系数。字体通过统一字体工厂管理；视觉尺寸需结合实际字体度量校准。

### 4.1 灰阶

| Token | 值 | 用途 |
| --- | --- | --- |
| paper | #FFFFFF | 主面板、表单、日志背景 |
| workspace | #F2F2F2 | 应用底色 |
| surface-muted | #E8E8E8 | 次级容器、禁用背景 |
| ink | #111111 | 主要文字、主轮廓 |
| black | #000000 | 标题黑块、硬边投影 |
| text-secondary | #595959 | 次要但仍需阅读的信息 |
| text-disabled | #777777 | 禁用文字；只配白或浅灰背景 |
| rule | #B8B8B8 | 非关键装饰分隔线，不承担唯一状态提示 |
| canvas | #242424 | 漫画预览周围背景 |
| on-ink | #FFFFFF | 黑底上的文字与图标 |

- 常规正文对比度以至少 4.5:1 为验收目标；关键控件轮廓与焦点以至少 3:1 为目标。
- 选中文本黑底白字；选中菜单项和标签页有固定形状提示。
- 禁用态同时禁用交互并解释原因，不只降低透明度。

### 4.2 几何

| 类型 | 默认值 |
| --- | --- |
| 间距刻度 | 4 / 8 / 12 / 16 / 24 / 32 |
| 页面边距 | 常规 24，紧凑窗口 16 |
| 面板内边距 | 常规 16，工具密集区 12 |
| 分隔线 / 控件边框 / 主分格 | 1 / 2 / 3 |
| 普通圆角 | 0；必要的小控件最多 2 |
| 特殊切角 | 6–8，仅重点按钮或状态标题 |
| 硬边投影 | 偏移 3,3，黑色实心，无模糊 |
| 主按钮 / 普通输入高度 | 40 / 36，文字缩放后允许增高 |
| 图标 / 图标按钮命中区 | 20 / 至少 32×32 |
| 分隔拖动命中区 | 6–8，显示线可更细 |

粗框只用于主要工作区或重点元素。不要给所有按钮、列表项和嵌套面板同时增加投影。

## 5. 字体与排版

### 5.1 字体家族

- 标题：优先思源黑体简体中文 Heavy/Bold，使用真实字重，保持汉字笔画清晰。
- UI 正文：优先思源黑体简体中文 Regular/Medium；缺失时回退 Noto Sans CJK SC、Microsoft YaHei UI、系统无衬线字体。
- 日文原文：配置对应日文字形字体，如思源黑体日文 / Noto Sans CJK JP，避免将所有 CJK 内容强制为简体字形。
- 日志英文、时间戳和编号：优先 JetBrains Mono，回退 Consolas、系统等宽字体。
- 日志中文：思源黑体简体中文 Regular；明确配置中文回退，不能只指定不含汉字的英文字体。
- 字体按实际安装或加载后的 family 名绑定，不能把字体文件名当作字体家族。
- 英文等宽加中文回退不保证中文恰好占两个英文字符宽；日志列使用字段布局或制表位置对齐，禁止依赖空格补齐。
- 随应用分发字体时保留其对应许可文件，控制包体；不默认打包庞大的完整 CJK 字体合集。

### 5.2 字级

以下为默认视觉目标，单位为逻辑像素；禁止在不同组件中随意混用 pt 与 px 造成不一致。

| 角色 | 大小 / 字重 | 规则 |
| --- | --- | --- |
| 品牌字标 | 26 / 800–900 | 可单独设计矢量字标 |
| 页面标题 | 24 / 800–900 | 每页最多一处最强标题 |
| 分区标题 | 17 / 700 | 可与编号或短黑条搭配 |
| 正文、输入、按钮 | 14 / 400–500 | 正文目标行高约 1.45 倍 |
| 辅助说明 | 12 / 400 | 不承担主要操作和错误原因 |
| 日志 | 14 / 400 | 目标行距约 21，允许 12–20 调整 |
| 阶段编号与统计 | 13–16 / 500–700 | 数字宽度稳定 |

- 禁止正文使用宋体、花体、手写体或大面积描边字。
- 装饰标题可短促倾斜或出框，但不拉伸汉字、不改变正文基线。
- 日志不加装饰字距，不全体加粗，不使用浅灰小字追求所谓终端感。
- Qt 文本控件的行距须用实际支持的文本布局机制实现，不能假设浏览器 CSS 的 line-height 可直接应用。
- 校验中文、日文、拉丁字母、全角标点、长路径、罕见字符和 100%/125%/150%/200% 系统缩放下的显示。

## 6. 组件与黑白状态

| 组件/状态 | 视觉与交互 |
| --- | --- |
| 主按钮 | 黑底白字、2px 描边；少量允许切角；悬停短暂抬起硬边投影 |
| 次按钮 | 白底黑字、2px 描边；悬停浅灰底 |
| 按下 | 内容或绘制层下移 1–2；预留空间，不挤动相邻布局 |
| 焦点 | 黑白双层焦点框，与选中态区分；键盘操作始终可见 |
| 标签页 | 选中项黑底白字或明确的黑色边条；采用同一种规则 |
| 输入 | 白底、清晰边框；错误时加错误图标与原因文字，不只改变线粗 |
| 等待 | 空心圆＋“等待”，保持可读文字 |
| 运行 | 黑色阶段头＋简单活动指示＋“运行中” |
| 成功 | 对勾图标＋“完成”，不整块染黑以免与运行混淆 |
| 警告 | 三角叹号＋“警告”，局部斜线或双线标题，可继续与否明确说明 |
| 失败 | 方形叉号＋“失败”＋原因；展开详细信息与可用恢复操作 |
| 跳过 | 短横图标＋“已跳过”及原因，不伪装成成功 |
| 停止中 | “正在停止”，反映后台真实收尾，禁止点击后立即假报停止 |
| 已取消 | 独立“已取消”标签，不计为成功或通用失败 |

- 所有图标使用统一的 24×24 设计网格、约 2 的描边宽度与一致端点风格。
- 禁止彩色 emoji 作为正式工具栏图标。现有品牌小人物若保留，应制作清晰的黑白版本。
- 图标按钮必须有工具提示与可访问名称。
- 危险操作通过具体文字和图标表达，不依赖红色，也不与“开始”采用相同邻接位置与形状而难以分辨。
- 滚动条保持可发现；不为装饰隐藏必要的滚动入口。

## 7. 流式输出：两个独立、联动的区域

### 7.1 流程进度区

- 呈现总体状态、已完成页数/总页数、当前页面、当前阶段与耗时。
- 单页阶段与整个批次完成量分别表达，避免一页完成被误判为整个任务完成。
- 实际总量未知时显示“不确定进度”与当前动作，不使用定时器虚构百分比。
- 模型初始化与预热作为真实前置状态；不混同于漫画页面处理进度。
- 阶段事件驱动状态变化；不依靠解析界面上截断后的日志文本确定状态。
- 如果现有后端只有日志文本，先建立兼容适配；新字段和事件必须接入真实处理逻辑后才能显示。

### 7.2 详细日志区

- 白底黑字，顶部细工具条提供级别筛选、搜索、复制、导出、跟随最新与字号调整；低频项可收进菜单。
- 记录字段建议：时间、级别、页面/文件、阶段、消息。短固定字段与可伸缩消息分开排布或使用固定制表位置。
- 显示示意：`13:32:36.241  INFO  第 03 页  OCR  检测到 12 个文本区域`。实际显示仅使用真实数据。
- 以收到的事件追加内容；按行输出优先。若翻译后端确实支持分片，更新同一条未完成记录，完成后固定。
- 分片并非新日志行；不能每个 token 生成一行，也不能重复显示累计全文。
- UI 层建议每 50–100ms 批量提交已收到记录；低频事件尽快显示，结束或失败时刷新剩余缓存。这是起始参数，需按实际吞吐调整。
- 默认跟随底部；用户向上滚动、选择文本或搜索历史时暂停跟随，显示“有 N 条新日志 · 回到最新”。
- 点击“回到最新”才恢复跟随。新日志不能清空用户选区或强制拖回底部。
- “暂停跟随”只改变视图；停止处理任务是另一个独立操作，文案不能写成含混的“暂停”。
- 长中文按宽度换行；长路径可换行并支持完整复制；续行尽量对齐消息正文。
- 级别用 INFO/WARN/ERROR 标签与黑白符号表达，符号只是装饰，复制时保留可理解的纯文本字段。
- 保留内存上限，默认显示最近 5,000 条逻辑记录；完整日志是否可导出必须由实际持久化支持。移除旧显示块不等于删除原始日志。
- 若采用段落上限，明确其统计单位为文本块而非业务记录，避免多行堆栈导致计数错误。
- 大量记录时使用增量追加、模型/视图或有界文本缓冲，禁止反复 setPlainText 重建所有历史。
- 工作线程/子进程退出、异常及通信中断均给出真实状态。仅在通信机制确实支持重连时显示重连状态。
- 日志无逐字打印动画，无闪屏、抖动、速度线背景、每行弹入或音效。

### 7.3 建议事件契约

逻辑上区分 `stage_changed`、`progress_changed`、`log_appended`、`log_fragment`、`task_finished`、`task_failed`、`task_cancelled`；名称可适配现有代码。

- 每条事件关联 task_id；需要时包含 page_id、stage_id、record_id 和有序序号。
- 切换任务或重试时隔离旧事件，防止前一任务迟到的信息写进当前视图。
- 后台执行耗时工作，GUI 主线程消费事件并更新控件。
- 在本地桌面应用中优先复用现有 Qt 信号/槽或进程消息，不为日志专门引入 WebSocket 服务。
- 处理完成与日志缓存清空的时序要协调，确保最终摘要不丢失。

## 8. 动效

| 场景 | 默认表现 | 时长 |
| --- | --- | --- |
| 按钮悬停/按压 | 轻微投影与位移 | 80–120ms |
| 侧栏、折叠区 | 平滑展开，结束后稳定 | 160–200ms |
| 页面切换 | 轻微位移或直接切换，不遮蔽任务状态 | 不超过 180ms |
| 导入完成 | 小气泡或编号章一次弹入 | 180–240ms |
| 全任务完成 | 小范围完成气泡或短速度线，文字继续保留 | 动效不超过 300ms |

- 不对每页完成重复播放庆祝动画；只在整个任务完成时触发一次。
- 不循环播放装饰动效，不闪烁错误，不震动窗口。
- 动画不能改变点击目标的位置或阻塞下一次操作。
- 支持“减少动态效果”，关闭装饰动画并保留静态状态反馈。
- 实际任务耗时不得因动画被人为延长。

## 9. 窗口适配

- 以约 1440×900 的逻辑工作区设计常态布局，在较小窗口与高 DPI 下验证。
- 可用宽度不足约 1180 时收紧边距，精修左侧缩略图可收起；再不足时右侧改为可切换面板，画布仍可操作。
- 不将 1920×1080 截图尺寸写成固定应用尺寸；使用屏幕可用几何范围限制默认窗口。
- 低高度窗口允许滚动表单与折叠阶段详情，开始、停止和当前状态不能被永久挤出。
- 面板宽度、日志高度、缩放比例和折叠状态在合理情况下持久化；更换显示器后将越界几何恢复到可见范围。
- 不通过缩小到难以阅读的字号来适配窗口。
- 绘图与图标处理遵循 Qt 的逻辑坐标和设备像素比，避免重复缩放。

## 10. AI 美术资产规范

允许执行改造的 AI 在任务范围内自行设计、制作、迭代配套美术资产，无需逐个资产确认。

### 10.1 制作方式

| 资产 | 优先方式 | 约束 |
| --- | --- | --- |
| 图标、气泡、切角、分格 | SVG / QPainter | 可缩放、形状可控、统一线宽 |
| 网点与斜线 | 平铺 SVG / 程序绘制 | 只用于装饰区域，不铺满正文 |
| 硬边投影 | Qt 绘制或偏移形状层 | 不用模糊图片模拟 |
| 上传、空任务、完成状态插画 | 有图像工具时生成栅格；简单图形可用 SVG | 黑白、留白明确、无内嵌功能文字 |
| 字体 | 现成字体文件 | 不用 AI 生成字形替代系统文本 |

- 没有图像生成能力时，使用可实现的矢量方案，不假装图片已经生成。
- 第一版资产集中于上传空状态、无预览状态、全任务完成三个位置。错误信息以真实文字为主。
- 网点默认点径约 1、间距 5 个逻辑像素；在实际缩放下检查摩尔纹，必要时改用更稀疏图案或取消。
- 常规任务页面装饰面积目标不超过约 8%，精修和日志工作区域不超过约 3%；空状态插画可独立占位。
- 同一视图同时出现的气泡、爆炸框、速度线等强装饰最多 1–2 处。
- 资产不含彩色、灰度渐变、旧纸颗粒、模糊光晕或摄影质感。
- 所有按钮、说明、状态、日志与译文必须由真实控件绘制，不能烘焙进图片。
- 导出的栅格资产只在需要透明背景时使用透明背景；实际检查边缘，无白边、锯齿、裁切或脏点。
- 优先复用现有品牌；现有彩色人物标志可作黑白适配，不擅自引入新的具名角色。
- 建议资产统一放入 assets/ui 下的 icons、patterns、illustrations、fonts；根据仓库已有目录规则调整。
- 通过项目资源与打包机制加载，确保已发布安装包离线可用；不依赖开发者电脑绝对路径或远程图片链接。

## 11. PySide6 落地边界

- 先识别现有项目使用 Qt Widgets 还是 Qt Quick；不能仅凭 PySide6 依赖推断全部界面实现。
- 若为 Qt Widgets：QSS 负责支持的基础颜色、边框、内边距与状态；QPainter/SVG 负责异形、纹理与特殊轮廓；布局与分隔器负责几何关系。
- QSS 不等同浏览器 CSS。不得直接写入 CSS Grid、Flexbox、transform、transition、clip-path、box-shadow 等并假设有效。
- 动效用 Qt 适配机制实现，不能依赖浏览器样式动画；初版优先保证交互与布局稳定。
- 简单高吞吐日志可用 QPlainTextEdit 的增量追加与段落上限；复杂字段布局可用模型/视图。依据真实需要选一种主实现，避免每行创建独立 QWidget。
- 根据已有线程或进程架构传递事件，后台不直接修改 GUI 控件。
- 字体、令牌、组件样式集中管理；QSS 若需令牌替换，在 Python 层生成，不假设支持 CSS 自定义变量。
- 自定义绘制只扩展必要外观，保留键盘焦点、命中范围、文本输入法和可访问名称。
- 使用与项目实际锁定 Qt 版本兼容的 API；当前 requirements 只提供版本下限，不应直接依赖后续版本新增接口。
- 不为实现漫画风将桌面主界面迁回旧版 Web 前端，也不在本轮同时维护两套新皮肤。

## 12. 执行与验收

### 实施顺序

1. 核对源码、现有页面、快捷键、任务状态和打包资源方式，建立旧入口到新入口映射。
2. 建立灰阶、字体、间距、线宽、图标与基础组件。
3. 完成自动处理页，接入真实阶段事件与流式日志，再完成精修、设置与诊断。
4. 基础行为稳定后加入少量装饰和动效，制作必要资产。
5. 在真实应用中检查布局、缩放与发布包资源，记录未支持的功能与偏差。

### 核心验收场景

- 空队列有明确导入入口，无巨大的无意义黑色预览占位。
- 长中文/日文文件名、长路径和多行报错可阅读与复制。
- 等待、预热、运行、完成、警告、失败、跳过、取消均不用颜色也能区分。
- 连续日志增长时界面仍可拖动、切页和选择文本；向上回看不被新日志拉回。
- 大批日志、最终错误、任务结束与快速切换任务不丢失终态、不串任务。
- 收起侧栏、缩小窗口和改变系统缩放后，关键操作可见，文本不截断。
- 已有翻译与嵌字业务行为正常，UI 换肤不改变图像数据、坐标和输出字体。
- 发布包能离线加载字体、图标与插画；不缺字、不显示彩色 emoji 替代图标。
- 最终需提供实际运行界面的截图，不能只用静态设计稿证明实现完成。

## 13. 禁用清单

禁止彩色强调、渐变、玻璃效果、发光、柔软大投影、大圆角卡片墙；禁止旧纸与抖线；禁止遍地气泡和速度线；禁止日志打字机动画、伪进度与假成功；禁止在正文背景铺网点；禁止以小字号掩盖拥挤；禁止把网页 CSS 原样当作 QSS；禁止用漫画风资产遮掩未实现功能。

## 14. 技术核对来源

这些来源用于核对实现边界，具体布局和数值是本设计的提案。文档可能展示较新 Qt 版本，编码时仍须检查项目兼容性。

- [Qt Style Sheets Reference](https://doc.qt.io/qt-6/stylesheet-reference.html)
- [Qt for Python: QPlainTextEdit](https://doc.qt.io/qtforpython-6/PySide6/QtWidgets/QPlainTextEdit.html)
- [Qt for Python: Threads and QObjects](https://doc.qt.io/qtforpython-6.10/overviews/qtdoc-threads-qobject.html)
- [Qt QFont](https://doc.qt.io/qt-6/qfont.html)
- [Qt High DPI](https://doc.qt.io/qt-6/highdpi.html)
- [Adobe Source Han Sans](https://github.com/adobe-fonts/source-han-sans)
- [JetBrains Mono](https://www.jetbrains.com/lp/mono/)

## 15. 下一轮审阅重点

视觉方向与重构范围已确认。建议用户阅读后优先反馈：新的两工作区导航、字体的粗细与大小、日志默认高度，以及精修界面截图与实际能力是否需要调整本提案。

## 16. 第二轮补充规则：漫画视觉强化（2026-09-15）

本节由用户在第一轮重构后明确追加，覆盖前文与本节冲突的装饰面积上限及过度克制要求。文字可读性、画布操作、真实任务状态和流式日志性能要求继续有效。基于现有功能和重构结果强化美术表现，不能只调整边框、圆角或添加空状态插画。

### 16.1 黑白构图与层级

- 每个主要页面必须有明确的视觉重心：黑色标题块、粗大章节编号或不对称分格。
- 主标题与正文形成明显尺度差；外部主分格约 3–4 个逻辑像素，内部细分隔约 1 个逻辑像素。
- 黑色块面用于标题、选中态和当前阶段的组织；避免所有面板采用相同粗框和投影。

### 16.2 常驻网点

- 自动处理主页面至少两处无需触发特殊状态就可见的网点，例如标题侧翼、队列顶部、流程区外围。
- 采用规则黑点与白底，起始点径 1.5–2、间距 6–8 个逻辑像素，普通窗口下清晰可辨。
- 网点以楔形、窄条、切角块或局部分格背景出现；不能整页平铺，不能穿过文字。
- 文字使用独立白底或黑底区域；不同 DPI 检查锯齿和摩尔纹，必要时降低密度。

### 16.3 受控破框

- 页面标题、当前阶段标题、完成反馈中，少量使用出框编号、切角标签、偏移硬影、气泡指向或斜切背景。
- 跨容器视觉边界的装饰不得遮挡文本、操作或缩小命中范围。
- 普通输入框、日志正文和编辑表单保持规整；不让所有按钮倾斜，不把每条信息装进气泡。

### 16.4 页面落地

- 自动处理：反白或粗黑主标题；队列顶部可见网点；当前阶段采用黑色标题块和放大编号。
- 精修：漫画感集中于顶栏、侧栏标题和工具分隔，画布与文字编辑区保持干净。
- 日志：标题条包含编号、分格和一小段网点，正文保持白底清晰，无纹理、逐字动画或抖动。

### 16.5 可复用资产

- 网点、切角、气泡和线条必须用实际可见的 SVG 或 Qt 自绘实现，统一点径、间距、线宽与硬影偏移。
- 装饰图层不截获鼠标事件、不改变控件命中区域。
- 保留黑白灰；禁止彩色、渐变、旧纸纹理、光晕与模糊投影。

### 16.6 本轮验收

- 先完成代表性的自动处理页面并检查实际效果，再将同一视觉语言应用到其他页面。
- 必须提供实际运行截图：自动处理空闲状态、处理中流式日志状态、精修页面。
- 常态不依赖漫画原图和品牌插画，也能看出漫画杂志风；正常尺寸清晰可见网点，缩小截图后仍有明确黑白块面与标题层次。

## 17. 网点与日志字体追加规则（2026-09-15）

本节记录用户在第二轮视觉强化后的追加要求；与前文冲突时以本节为准。

- 网点从容器边缘起，以交错的点行堆砌成三角形，向内部逐渐稀疏。通过逐步增加间距改变密度，保留纯黑圆点和白底，不能用灰色透明渐变代替。
- 标题侧翼从右边缘向内收尖；队列、精修侧栏顶部从上边缘向下收尖。所有网点继续使用可复用 Qt 自绘组件，文字与操作区保持独立。
- 日志正文采用清晰的漫画手写风字体，优先使用项目已有的霞鹜文楷；时间戳、日志级别保留等宽字体便于对齐。此项覆盖前文对日志使用手写字体的限制。
- 日志默认 16 px，保留用户已保存的字号设置及字号调整功能；字体不可用时回退到常规中文界面字体。
- 流式输出沿用真实后台消息的批量增量追加，保持白底、可选择、可搜索、可复制和回看位置。不增加逐字动画、抖动或日志背景纹理。

## 18. 网点精确参数（用户最新补充，2026-09-15）

本节覆盖前文冲突的网点颜色、透明度、方向、尺寸、间距及禁止渐隐要求；日志字体与流式性能要求仍有效。

- 类型：漫画半调网点 / 波普点阵；圆点沿 45° 斜向网格排列。
- 从左上到右下由密到疏：点径 2 → 0.6 px，间距 8 → 18 px。
- 颜色 #111，基础透明度 15% → 8%；叠加线性渐隐或中心向外渐隐遮罩。
- multiply 正片叠底；pointer-events: none，不接管鼠标事件。
- 小屏间距额外增加 4 px；当前桌面实现以窗口宽度低于 960 个逻辑像素判定。
- 采用内联 SVG，不使用位图；Qt 界面通过 QSvgRenderer 绘制，QPainter 设置 Multiply 实现对应混合模式。
- 保留边缘起始的三角构图与独立文本底面。SVG 将遮罩值写入圆点透明度，避免不同渲染器对遮罩产生不同结果。

## 19. 实际参考图优先：网点与大字体张力（2026-09-15）

用户随后提供实际样式参考图，强调网点使用、样式和大字体张力。本节以该参考图为最新视觉依据，覆盖第 18 节与之冲突的低透明度、小点径等参数；参考图中的文件、页数、进度与功能仅为画面内容，不作为业务数据或新增功能要求。

- 自动处理页采用横跨队列与工作区的通栏标题。主标题使用本地思源黑体 Heavy，正常窗口约 58 px；章节编号为白底黑描边的大号倾斜文字，允许沿标题边界裁切。
- ComiTrans 字标使用更大的粗斜体；主标题、区域标题、正文应有明确尺度差。
- 网点采用不透明黑色圆点，密集端直接形成可见块面。标题左右边缘设置网点，右侧由边缘向中央留白收疏；文字始终位于独立底面。
- 标题网点点径约 4.8 → 0.6、间距约 7 → 11；分区网点点径约 3.2 → 0.6、间距约 6 → 10，均按逻辑像素绘制。以直径和密度变化表达消散，不再使用 8%–15% 的浅灰网点。
- 保留 45° 斜向点阵、内联 SVG、multiply 混合、缓存和鼠标穿透；小窗口增加间距，避免摩尔纹。
- 队列和精修侧栏使用粗大黑字与局部网点；日志与预览标题采用反白标题块和网点侧翼。阶段编号增至 24 px，实际运行阶段使用黑色选中块。
- 日志正文继续使用霞鹜文楷，保持真实流式输出、白底可读和选区/回看稳定；画布与编辑字段保持清晰规整。
- 较小窗口压缩通栏标题、收起重复阶段卡片，保留当前阶段与关键操作；不得以字体张力为由覆盖控件。

## 20. 节点轨道、日语角标与指定字体（2026-09-15）

本节保存用户在参考图调整后追加的要求，覆盖此前冲突的阶段卡片、字标与日志字体要求。

- 阶段流程改为一条水平直线与七个圆点，不再使用卡片墙或折行网格。通过节点后填充对应轨道，等待为空心、运行中为内实心点、完成为黑底勾选、跳过为短横线；失败与取消有独立状态。
- 填充依据当前页面收到的真实阶段结果。完成或明确跳过后通过节点，不以计时模拟进度；中间节点未完成或失败时不越过该节点。并行页面分别维护状态，迟到的运行事件不撤销已完成或跳过结果。
- 删除阶段轨道上方重复的文件进度条，保留真实已处理页数与文件数量。小窗口仍显示单行轨道，压缩状态文字及标题高度以保持关键操作可见。
- 标题角标改成日语「漫画翻訳」「一頁ずつ磨く」。
- 左上角 ComiTrans 使用用户提供 Skribble.zip 中的 Skribble 字体，不额外合成粗体或斜体。
- 流式日志正文使用用户提供 AaJinRiHuaQing-ChunLanMaoKun.zip 中的「Aa今日花晴 春懒猫困」。默认 18 px 便于阅读，保留用户已保存的字号；时间戳和级别保留等宽对齐。
- 两个字体原样保存至 assets/ui/fonts，通过 Qt 应用字体机制按实际字体族名注册；与漫画输出嵌字字体配置独立。保留原始内嵌版权信息，不导入字体包中的网站快捷方式。
- 保持每 75 ms 增量批量追加、记录上限、搜索、复制、选区与回看位置等日志行为。

## 21. 紧凑控制区与快速逐字日志（2026-09-15）

用户最新要求：压缩进度条模块、输出目录和原文语言部分；日志改为快速逐字输出。本节覆盖前文禁止日志打字机动画和每 75 ms 整行批量显示的要求，其他可读性与性能约束继续有效。

- 当前阶段标题高度压缩为 32 个逻辑像素，节点轨道正常高度 54、小窗口 36。保留直线、节点与真实通过状态。
- 原文语言、翻译偏好、输出目录与开始按钮在宽度至少 1180 时合并一行；窄窗口自动换行。缩小控件内边距和间距，文字保持 13 px；不影响精修表单。
- 日志沿用指定 Aa 字体，正文按约每秒 250 字快速展开，以 16 ms 定时器分帧追加。积压较多时自动加速，不为每个字符创建控件或定时器。
- 完整日志记录与显示进度分开保存。报错、任务终止、复制或导出时立即补齐剩余文字，保留多行、搜索和最多 5,000 条记录的上限。
- 选区或回看时暂停跟随并补齐文字，保持选区和滚动位置；组合字符与连接表情不在显示帧边界拆开。日志正文继续使用白底，不加入纹理或抖动。
- 逐字效果只呈现后台已收到的真实消息，不伪造服务端 token 流或任务进度。通过真实 Qt 窗口截图及定时器录屏验证。
